# Base de datos (PostgreSQL / Neon)
# La app usa DATABASE_URL. Prisma necesita además DIRECT_URL para migraciones.
DATABASE_URL="postgresql://usuario:password@host:5432/dbname?sslmode=require"
# Para migraciones: si usas Neon, en el dashboard hay "Connection string" (pooled) y "Direct connection".
# Puedes usar el mismo valor que DATABASE_URL si no tienes conexión directa separada.
DIRECT_URL="postgresql://usuario:password@host:5432/dbname?sslmode=require"
